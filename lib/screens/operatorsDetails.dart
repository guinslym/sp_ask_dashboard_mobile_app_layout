import 'package:flutter/material.dart';

class operatorsDetailsScreen extends StatelessWidget {
  const operatorsDetailsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          Center(
            child: Text("operatorsDetailsScreen"),
          ),
        ],
      ),
    );
  }
}
