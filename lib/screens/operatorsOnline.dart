import 'package:flutter/material.dart';

class operatorsOnlineScreen extends StatelessWidget {
  const operatorsOnlineScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          Center(child: Text("operatorsOnlineScreen"),),
        ],
      ),
    );
  }
}